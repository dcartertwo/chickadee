import { type FC } from "hono/jsx";
import type { IDimensionBars } from "../../lib/models";

const DimensionsCardSource: FC<{
  dimensions: IDimensionBars[];
}> = ({ dimensions }) => {
  
};

export default DimensionsCardSource;
